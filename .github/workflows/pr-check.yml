name: pr-check request

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  pr-check:
    runs-on: ubuntu-latest
    steps:
      - name: PR check running for branch
        run: echo "PR check running for branch:" ${{ github.head_ref }}

            